class User {
  final String id, email, name, username, avatarUrl;

  User(
      {required this.id,
      required this.email,
      required this.name,
      required this.username,
      required this.avatarUrl});
}
